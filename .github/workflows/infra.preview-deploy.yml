name: "Infra Preview Deploy"

on:
  push:
      branches:
      - main
      paths:
      - 10_infra/**
      - .github/workflows/infra.preview-deploy.yml
  workflow_dispatch:

  permissions:
    contents: read
    id-token: write

jobs:
  preview:
    name: "Preview"
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello world!"